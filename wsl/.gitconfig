[user]
	name = Jon Lighthall
	email = jon.lighthall@gmail.com
[credential]
	helper = cache --timeout 172800
	helper = store --file ${HOME}/.git-credentials	
[core]
	autocrlf = input
	fileMode = true
[color]
	ui = true
[push]
	default = simple
[pager]
	pager = less -FX
